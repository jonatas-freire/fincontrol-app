import styled from 'styled-components'

export const Main = styled.main`
  max-width: 420px;
  min-height: 100vh;
  margin: auto;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  background: transparent radial-gradient(closest-side at 50% 50%, #FFFFFF 0%, rgb(242 242 242 / 20%) 100%) 0% 0% no-repeat padding-box;
`

export const Spacing = styled.div`
  margin-bottom: 10px;
  width: 100%;
`